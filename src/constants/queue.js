const queueName = {
  DEFAULT: 'DEFAULT',
};

const queueNameArr = Object.values(queueName);

const workerName = {
  DEFAULT: 'default',
};

module.exports = {
  queueName: queueName,
  queueNameArr: queueNameArr,
  workerName: workerName,
};
